/**
 * Knowledge Ingestion Service
 * Handles catalog sync, chunking, embedding, and vector indexing
 */

export function initIngestionService() {
  console.log('@fluxbot/ingestion-service initialized');
}
