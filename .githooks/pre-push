#!/bin/sh
set -eu

echo "[pre-push] Running mandatory checks..."
npm run typecheck --workspace @fluxbot/shopify-admin-app
npm test --workspace @fluxbot/shopify-admin-app

echo "[pre-push] OK: typecheck + unit tests passed."
